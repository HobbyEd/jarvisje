# Core lightweight dependencies for Sogyo Chatbot MVP
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
trafilatura>=1.12
sentence-transformers>=3.1,<4.0
chromadb>=0.5
pydantic>=2.7
tqdm>=4.66
python-dotenv>=1.0

# Optional but recommended for polite scraping
# robotexclusionrulesparser>=1.7.1
# For now using stdlib urllib.robotparser

# Dev / testing
pytest>=8.0

# API (FastAPI + SSE)
fastapi>=0.115
uvicorn[standard]>=0.30
